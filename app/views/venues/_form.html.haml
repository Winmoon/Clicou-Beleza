= simple_form_for(@venue, defaults: { input_html: { class: 'form-horizontal' } }) do |f|
  = f.error_notification
  = f.input :name
  = f.input :venue_id
  = f.input :deleted_at
  .form-actions
    = f.button :submit, :class => "btn-primary"
