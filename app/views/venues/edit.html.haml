.content-header
  .pull-left
    %h2 #{t('actions.edit')} #{t('activerecord.models.venue').downcase} - #{@venue}
  .pull-right
    .btn-group
      = link_to venues_path, class: 'btn', title: t('actions.back') do
        %i.icon-arrow-left
      = link_to @venue, class: 'btn tip-bottom', title: t('actions.show') do
        %i.icon-eye-open
      = link_to new_venue_path, class: 'btn btn-success', title: "#{t('actions.create')} #{t('activerecord.models.venue').downcase}" do
        %i.icon-plus.icon-white
  .clearfix
  
%ul.breadcrumb
  %li
    = link_to t('views.home'), root_path
    %span.divider /
  %li
    = link_to t('activerecord.models.venue').pluralize, venues_path
    %span.divider /
  %li.active #{t('actions.edit')} #{t('activerecord.models.venue').downcase} - #{@venue}

.content-body
  = render 'form'